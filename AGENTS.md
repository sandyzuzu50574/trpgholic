# ZUZU TRPG-holic deployment

- GitHub repository: `sandyzuzu50574/trpgholic`
- The public website is served by GitHub Pages from the `main` branch.
- When the user asks to「公開」, build and validate the GitHub Pages export, commit the exact validated source, push `main` to the GitHub remote, and verify that the GitHub Pages workflow succeeds.
- Do not report the public update as complete until the GitHub push and Pages deployment are both verified.
- ChatGPT Sites is not the canonical public deployment unless the user explicitly asks to publish there.
